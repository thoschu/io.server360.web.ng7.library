import { TestBed } from '@angular/core/testing';

import { PsImageDisclaimerNg7LibService } from './ps-image-disclaimer-ng7-lib.service';

describe('PsImageDisclaimerNg7LibService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: PsImageDisclaimerNg7LibService = TestBed.get(PsImageDisclaimerNg7LibService);
    expect(service).toBeTruthy();
  });
});
